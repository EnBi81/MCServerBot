/**
 * Server status enum.
 * @type {{Starting: string, ShuttingDown: string, Offline: string, Online: string}}
 */
const ServerStatus = {
    Online: "online",
    Offline: "offline",
    ShuttingDown: "shutting-down",
    Starting: "starting",
}